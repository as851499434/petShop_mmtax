<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mmtax.business.mapper.AddressMapper">

    <resultMap type="Address" id="AddressResult">
        <result property="id" column="id"/>
        <result property="address" column="address"/>
        <result property="addresseeName" column="addressee_name"/>
        <result property="mobile" column="mobile"/>
        <result property="status" column="status"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="providerId" column="provider_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <select id="getAddressByMerchantId" resultMap="AddressResult">
        SELECT *
        FROM tbl_address
        WHERE merchant_id = #{merchantId}
          AND status = 0;
    </select>
    <select id="getListInvoiceAddress" resultType="com.mmtax.business.dto.ManagerInvoiceAddressDTO">
        select
        t1.id as id,
        t1.mobile as mobile,
        t1.merchant_id as merchantId,
        t1.address as address,
        t1.addressee_name as addresseeName,
        t2.merchant_name as merchantName,
        t3.user_name as saleName
        from tbl_address t1
        left join tbl_merchant_info t2 on t1.merchant_id = t2.id
        left join sys_user t3 on t2.belong_saler = t3.user_id
        where 1=1
        and t1.status='0'
        <if test="saleId != null and saleId != ''">
            and t2.belong_saler = #{saleId}
        </if>
        <if test="merchantName != null and merchantName != ''">
            and t2.merchant_name like concat('%',#{merchantName},'%')
        </if>
        <if test="saleName != null and saleName != ''">
            and t3.user_name like concat('%',#{saleName},'%')
        </if>
        <if test="mobile != null and mobile != ''">
            and t1.mobile like concat('%',#{mobile},'%')
        </if>
        <!-- 数据范围过滤 -->
        ${params.dataScope}

        order by t1.id desc
    </select>
</mapper>