<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mmtax.business.mapper.MerchantKeyMapper">

    <resultMap type="MerchantKey" id="MerchantKeyResult">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="whiteUrl" column="white_url"/>
        <result property="callBackAddress" column="call_back_address"/>
        <result property="appKey" column="app_key"/>
        <result property="desKey" column="des_key"/>
        <result property="providerId" column="provider_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="getMerchantKeyByMerchantId" resultMap="MerchantKeyResult">
        select *
        from tbl_merchant_key
        where merchant_id = #{merchantId}
    </select>

    <select id="getListManagerSeretKey" parameterType="com.mmtax.business.dto.ManagerSeretKeyDTO"
            resultType="com.mmtax.business.dto.ManagerSeretKeyDTO">
        select
        t2.merchant_name as merchantName,
        t2.merchant_code as merchantCode,
        t1.create_time as createTime,
        concat(left(t1.app_key,6),'******',right(t1.app_key,6)) as appKey,
        concat(left(t1.des_key,6),'******',right(t1.des_key,6)) as desKey,
        t1.id as id,
        t3.user_name as saleName
        from
        tbl_merchant_key t1
        left join
        tbl_merchant_info t2
        on t1.merchant_id = t2.id
        left join sys_user t3
        on t2.belong_saler=t3.user_id
        where
        1=1
        <if test="merchantName != null and merchantName != ''">
            and t2.merchant_name like concat('%',#{merchantName},'%')
        </if>
        <if test="merchantCode != null and merchantCode != ''">
            and t2.merchant_code like concat('%',#{merchantCode},'%')
        </if>
        <if test="startDate != null and startDate != '' and endDate != null and endDate != ''">
            and t1.create_time between concat(#{startDate},' 00:00:00') and concat(#{endDate},' 23:59:59')
        </if>
        <if test="saleId != null and saleId != ''">
            and t2.belong_saler = #{saleId}
        </if>
        <if test="saleName != null and saleName != ''">
            and t3.user_name like concat('%',#{saleName},'%')
        </if>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by
        t1.create_time desc
    </select>

</mapper>